Tag — small uppercase pill used to label a post's topic (e.g. "AI Governance", "Talks").

```jsx
<Tag tone="accent">AI Governance</Tag>
<Tag>Enterprise Risk</Tag>
```

Use `accent` tone sparingly — for the one topic that matters most on a card — and `default` for the rest, so tags don't compete with the content.
