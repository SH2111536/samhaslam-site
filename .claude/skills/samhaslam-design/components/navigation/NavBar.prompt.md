NavBar — top-of-page navigation: italic wordmark on the left, section links on the right.

```jsx
<NavBar current="writing" />
```

The active link (matching `current`) is full-brightness text with an accent underline; inactive links sit at secondary text color. Only four links exist by design — Home, About, Writing, Contact — matching the site's four sections.
